# ROS2 Workspace Source Root

ROS2 packages live in this workspace root.

Current package:

- `configflux_ros2_sdk`: runtime-backed parameter/control/diagnostics adapters plus
  node callback integration for ROS2 wrappers.
