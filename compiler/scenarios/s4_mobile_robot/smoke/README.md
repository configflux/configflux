# S4 Mobile Robot Smoke

Smoke profile for Loop 5 early-binding validation.

Focus areas:
- mixed lifecycle partitioning (`construction`, `startup`, `runtime`)
- hardware-heavy artifact selection for build-time generation
- deterministic early-binding output artifacts
